# OpenAI API client used for GPT-4o vision and text-only AI extraction
openai>=1.3.0
# Loads environment variables from a .env file
python-dotenv>=1.0.0
# Provides beautiful console output, progress bars, and interactive CLI tables
rich>=13.0.0
# Parses PDF files to extract the embedded text layer and convert pages to high-resolution images
PyMuPDF>=1.23.0
# Web framework for the backend API
fastapi>=0.100.0
# ASGI server for FastAPI
uvicorn>=0.23.0
# Required by FastAPI for file uploads
python-multipart>=0.0.6
# Generates Word documents from the extracted Markdown
python-docx>=0.8.11
